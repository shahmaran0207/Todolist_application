import React, {useState} from "react";
import './App.css'

//1. 인풋창 + 버튼
//2. 인풋창에 값 입력 -> 버튼 -> 아이템 추가
//3. 아이템 삭제 버튼 -> 삭제

function App(){

    return(
        <main>
            <input type="text"/>
                <button></button>
        </main>
    );
}

export default App;